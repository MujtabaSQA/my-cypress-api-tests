describe("Test Suite", function () {
    
    it('does things', function () {
        var objectA = {
            lastName: 'Daniels',
            employeeId: 1224,
            firstName: 'Dylan',
        };
        var objectB = {
            employeeId: 1224,
            firstName: 'Dylan'
        };
        objectA.employeeNickname = 'Dylan';
        objectA.employeeHobby = 'yodeling';
        objectB.lastName = 'Daniels';
        objectB.employeeNickname = objectA.employeeNickname;
        objectA.favoriteColors = ['red', 'blue', 'pink', 'purple', 'navy', 'gray'];
        objectA.employeeHistory = {};
        objectA.employeeHistory.yearsWorked = 2;
        objectA.employeeHistory.warnings = 0;
        objectA.employeeHistory.monthsWorked = 6;
        objectB.employeeHobby = objectA.employeeHobby;
        objectB.employeeHistory = objectA.employeeHistory;
        objectB.favoriteColors = ['red', 'blue', 'pink', 'green', 'navy', 'gray'];
        objectB.employeeHistory.monthsWorked = 7;
        expect(objectB).toEqual(objectA);
        console.log(objectB);
        console.log(objectA);
        expect(objectB).jsonToEqual(objectA);
   }); 


   it('does things with jsonCompare', function () {
        var objectA = {
            hello: 'taco',
            poop: 'scoop'
        };
        var objectB = {
            poop: 'why',
            hello: ['taco']
        };
        var objectC = {
            hello: 'taco',
            poop: 'scooper',
            blah: 'blah'
        };
        var objectD = {
            yes: 'no',
            hi: {
                yin: 'yang',
                greeting: 'elephant'
            }
        }
        var objectE = [
            'my',
            'name',
            'is',
            'fred'
        ];
        expect(objectA).jsonToEqual(objectB);
        expect(objectA).jsonToEqual(objectC);
        expect(objectD).jsonToEqual(objectA);
        expect(objectE).jsonToEqual(objectB);
        expect(objectA).jsonToEqual(objectE);
   });    



    // it('compares two big things', function () {
    //     var alphabet = "abcdefghijklmnopqrstuvwxyz".split("");
    //     var one = {};
    //     var two = {};
    //     alphabet.forEach(function (key) {
    //         one[key] = Math.random();
    //         two[key] = Math.random();
    //     });
    //     expect(one).jsonToEqual(two);
    // });
});

